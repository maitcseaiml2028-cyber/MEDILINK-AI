services:
  - type: web
    name: health-stack-manager
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: SESSION_SECRET
        generateValue: true
